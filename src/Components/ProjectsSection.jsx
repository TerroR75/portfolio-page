import { useState } from 'react';

function ProjectsSection() {
  const [count, setCount] = useState(0);

  return <section className='Projects-Section'>Proj section</section>;
}

export default ProjectsSection;
